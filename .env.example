# Local development uses SQLite automatically when DATABASE_URL is empty.
# For Vercel/Supabase, set DATABASE_URL to the Supabase Postgres connection
# string. The transaction pooler URL is recommended for serverless.

DATABASE_URL=

# Optional. Leave default SSL on for Supabase; use "disable" only for local
# Postgres instances without SSL.
# PGSSLMODE=disable
